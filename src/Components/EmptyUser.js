import React from 'react';
import { Typography } from '@material-ui/core';

const EmptyUser = () => (
  <Typography variant="h5">
    Here will be a profile from Github. Enter above a Github login to see more.
  </Typography>
);

export default EmptyUser;
