import React from 'react';
import { Typography } from '@material-ui/core';

const NotFound = () => (
  <Typography variant="h5">
      User that you are looking for not exist
  </Typography>
);

export default NotFound;
